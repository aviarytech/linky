# paper-wallet

## Install

`yarn install`

`yarn install --production`

## Start

Start dev server

`yarn dev`

Make new build

`yarn build:dev`

`yarn build:prod`

Start express server

`yarn start`
